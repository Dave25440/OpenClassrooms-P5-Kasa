import Collapse from "../Collapse";

const CollapseContainer = () => {
    return (
        <section>
            <ul>
                <Collapse />
            </ul>
        </section>
    );
};

export default CollapseContainer;
